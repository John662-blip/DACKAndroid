<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Màu nền -->
    <solid android:color="@android:color/transparent" />

    <!-- Viền -->
    <stroke
        android:width="2dp"
        android:color="#888888" /> <!-- màu viền nhẹ -->

    <!-- Bo góc -->
    <corners android:radius="8dp" />
</shape>
